export const Setting = {
  CardsCount: 6
};

export enum AppRoute {
  Login = '/login',
  Offer = '/Offer',
  Favorites = '/Favorites',
  Root = '/',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
